---
name: Beaker Vue Type Check PRs

# yamllint disable-line rule:truthy
on:
  pull_request:
    paths:
      - 'beaker-vue/src/**'
    types: [opened, synchronize, reopened]

jobs:
  type-check:
    name: Type Check beaker-vue
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: 'beaker-vue/package-lock.json'

      - name: Install dependencies
        working-directory: beaker-vue
        shell: bash
        run: npm ci

      - name: Run type check
        working-directory: beaker-vue
        shell: bash
        run: npm run type-check
